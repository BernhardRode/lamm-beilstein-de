language: node_js
node_js:
  - 'stable'
cache:
  directories:
    - node_modules
branches:
  only:
    - master
deploy:
  provider: pages
  skip-cleanup: false
  github-token: $GITHUB_TOKEN
  local_dir: public
  keep-history: false
  on:
    branch: master
script:
  - 'npm run deploy'
