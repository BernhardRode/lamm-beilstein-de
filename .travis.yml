language: node_js
node_js:
  - 'stable'
deploy:
  provider: pages
  skip-cleanup: false
  github-token: $GITHUB_TOKEN
  local_dir: public
  keep-history: false
  on:
    branch: master
branches:
  only:
    - master
script:
  - 'npm run deploy'
